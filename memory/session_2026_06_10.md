# Session 2026-06-10

## PANEN RISOL evidence untuk estimasi penyebab

- `summary_helpers.py` menambahkan `_panen_risol_evidence_hint()` sebagai rule evidence heuristik terinspirasi workbook `PANEN RISOL (Responses).xlsx`.
- Fitur yang dipakai: `R`, `X`, `R/X`, `X/R`, rasio `3I0/loop`, rasio `3V0/loop`, dan beda sudut loop dari fasor/hasil SE.
- Evidence PANEN RISOL menambah skor kandidat secara konservatif:
  - `Resistif-Pohon` / `Tegakan Non-Pohon` -> `Vegetasi / Pohon`.
  - `Hewan` -> `Satwa Liar (Bird Streamer)`.
  - `Petir` -> `Sambaran Petir / Flashover`.
  - `Flying Object` -> kandidat baru `Benda Asing / Flying Object`.
- Jika fitur tidak masuk window spesifik tetapi `R` dominan atau `X` dominan, Summary memberi bonus lemah: `R` dominan -> Vegetasi/Pohon, `X` dominan -> Sambaran Petir/Flashover. Ini mencegah evidence PANEN RISOL terlihat tidak berpengaruh saat kasus berada di luar ambang empiris PANEN RISOL.
- Summary dan Machine Learning memakai fallback `two_ended_local_single_result` bila `single_ended_result` lokal belum pernah dihitung, sehingga evidence PANEN RISOL tetap aktif setelah user hanya menghitung DE.
- Bila skor kandidat belum decisive, Summary memakai label eksplisit `Belum pasti: <kandidat terkuat>` alih-alih label generik agar user melihat objek/kandidat penyebab yang sedang bersaing.
- Guardrail: rule PANEN RISOL adalah bukti pendukung, bukan penyebab final otomatis. Penyebab final tetap perlu data eksternal/validasi lapangan.

## Line Parameter UX dan Fragment Rerun

- Baris `line_impedance` yang cocok dengan filter sidebar tetapi Z1/Z0 kosong/tidak valid sekarang tampil sebagai alert ramah di tab Line Parameter.
- Alert menampilkan baris terpilih, detail error singkat, dan kolom impedansi yang terdeteksi, lalu membuka input manual agar user tetap bisa lanjut.
- Guardrail: jangan biarkan `ValueError` dari `extract_impedance_from_row()` untuk data Z1/Z0 kosong tampil sebagai traceback `Gagal membaca database spreadsheet`.
- Normalize Line Parameter tetap menyimpan `line_param`, `line_param_df`, dan `effective_line_param` ke `st.session_state`.
- Karena `tabs/line_parameter.py` memakai `@st.fragment`, Normalize sukses wajib memanggil `st.rerun(scope="app")` agar tab DE/SE/HR/R-X membaca state terbaru. Tanpa full rerun, halaman DE bisa masih menampilkan warning `Selesaikan dulu Step 7: Line Parameter`.

## General Save ke Cloud

- Tombol `Simpan Case ke Cloud` di sidebar dan Setup DB sekarang menjadi tombol general save.
- Alur general save:
  - simpan case ZIP/payload ke `saved_cases` dan `saved_cases_data`;
  - upsert isian `Machine Learning > Dataset Penyebab` ke `fault_cause`;
  - upsert isian `Machine Learning > Kalibrasi Lokasi` ke `fault_location` bila `two_ended_result` sudah tersedia.
- Jika Double-End belum dihitung, bagian `fault_location` dilewati dengan pesan status; penyimpanan case dan `fault_cause` tetap diproses.
- Guardrail: tombol cloud harus memakai helper general save bersama agar tombol sidebar dan tombol Setup DB tetap konsisten.
